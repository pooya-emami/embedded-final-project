# Security Server Configuration

FRAME_INTERVAL_MS = 100
FRAME_WIDTH = 320
FRAME_HEIGHT = 240

PORT_HTTP  = 8080
PORT_HTTPS = 8443

MAX_HISTORY = 5

TEMP_THROTTLE_C = 65
MIN_INTERVAL_MS = 200
WATCHDOG_TIMEOUT_MS = 500
