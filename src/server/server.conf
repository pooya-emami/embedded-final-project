# Security Server Configuration

FRAME_INTERVAL_MS = 33
FRAME_WIDTH = 320
FRAME_HEIGHT = 240

PORT_HTTP  = 8080
PORT_HTTPS = 8443

MAX_HISTORY = 5

TEMP_THROTTLE_C = 65
MIN_INTERVAL_MS = 200
WATCHDOG_TIMEOUT_MS = 500

# SMTP - Email alerts
SMTP_SERVER=smtp://smtp.gmail.com:587
SMTP_USER=pooyaemamivar77@gmail.com
SMTP_PASS=ihlggpscsihpyzlu
SMTP_TO=pooyaemamivar77@gmail.com

# MQTT - All credentials here, NOT hardcoded
MQTT_HOST=127.0.0.1
MQTT_PORT=1883
MQTT_USER=security_user
MQTT_PASS=secure_password_here